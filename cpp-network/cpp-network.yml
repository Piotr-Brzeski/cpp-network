targets:
  cpp-network:
    platform: macOS
    type: library.static
    sources:
      - path: descriptor_set.cpp
        group: cpp-network
      - path: descriptor_set.h
        group: cpp-network
        buildPhase: none
      - path: exception.h
        group: cpp-network
        buildPhase: none
      - path: message.h
        group: cpp-network
        buildPhase: none
      - path: notifier.cpp
        group: cpp-network
      - path: notifier.h
        group: cpp-network
        buildPhase: none
      - path: select.cpp
        group: cpp-network
      - path: select.h
        group: cpp-network
        buildPhase: none
      - path: server.cpp
        group: cpp-network
      - path: server.h
        group: cpp-network
        buildPhase: none
      - path: socket.cpp
        group: cpp-network
      - path: socket.h 
        group: cpp-network
        buildPhase: none
